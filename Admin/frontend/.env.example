# Admin frontend API - point to the Admin backend base URL
# Note: API routes in api.jsx already include /admin prefix
# So this should be just the base URL without /admin
VITE_ADMIN_API_URL=http://localhost:5000
